# Feedback Examples

These files are reproducible demo inputs and can be committed.

Runtime feedback collected during local runs should stay in:

```text
artifacts/feedback/feedback.jsonl
```

Demo run:

```powershell
python scripts\run_real_agent.py --feedback-path examples\feedback\first-real-agent.feedback.jsonl
```
